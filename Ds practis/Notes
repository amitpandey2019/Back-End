What is the data 
--The Quantites, character or symbols on which oprations are performed by a
--computer, which may be a stored and transmitted in the form of electrical signals 
--and recorded on magnetic, optical or mechanical recordind media.

Example:-
-- c = a+b

When data becomes information?
-- DATA: PANDEY SI EMAN YM--(Collection of characters)
-- INFORMATION: MY NAME IS AMIT --(if data is arranged in a systemtic way then it gets structure and become meaningful )
-- To provide an appropriate way to structure the data, we need to know about.
Example: Arrays --
         multiple variables
         Storing Storing

        (Example1)---

--Stack data structure is used in implementing redo and undo feature.
.ed , Quality, 
undo Stack(cation)

1. LIST INTERFACE--LIST--1. Arraylist
                       --2. LinkedList
                       --3. Vector(Stack)

2. QUEUE INTERFACE(FIFO)--QUEUE--1.PriorityQueue
                               --2.LinkedList
                               --3.Deque(ArrayDeque)
                               
3. SET INTERFACE--SET--1.HashSet
                     --2.LinkedHashSet
                     --3.SortedSet(TreeSet)
        
4. MAP INTERFACE--MAP--1.HashMap
                     --2.LinkedHasMap
                     --3.SortedMap(TreeMap)
                     --4.Hashtable